This is the build folder for Make builds.
In order to construct and then run a Make build, please run:

$ cmake ../../

and then:

$ make

from within this folder in order to first construct the Make
build, and then run it.
